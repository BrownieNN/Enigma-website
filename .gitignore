# Internal working docs — keep OUT of the repo and the public site
# (they'd otherwise be reachable at /SAVE_STATE.md etc. and contain candid notes)
SAVE_STATE.md
PRODUCT.md

# OS / editor cruft
.DS_Store
*.swp

# Netlify local state
.netlify/

# Node (none yet, but safe)
node_modules/
.env
.env.*
